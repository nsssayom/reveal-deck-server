{
  "name": "reveal-deck-server",
  "version": "0.1.0",
  "private": true,
  "description": "Host multiple reveal.js decks and drive one device from another via Socket.IO multiplex.",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "socket.io": "^4.7.5"
  }
}
